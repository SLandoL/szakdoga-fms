@page
@model AccessDeniedModel
@inject IStringLocalizer<App> Localizer
@{
    ViewData["Title"] = "Access denied";
}

<header>
    <h1 class="text-danger">@ViewData["Title"]</h1>
    <p class="text-danger">@Localizer["Nincs hozzáférése ehhez az erőforráshoz."]</p>
</header>
